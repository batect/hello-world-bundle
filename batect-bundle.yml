containers:
  hello-world:
    image: alpine:3.16.0

tasks:
  say-hello:
    description: Say hello to the world.
    group: Greetings
    run:
      container: hello-world
      command: echo "Hello world!"
